/**
* Stores all DOM elements with which it we will interact.
* @return {object} Total DOM elements
*/
export const domElements = () => {
    const elements = {
      $btnNuevoPedido: document.querySelector('#btn-nuevo-pedido'),
    }
  
    return elements
}